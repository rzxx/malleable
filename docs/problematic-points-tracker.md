# Problematic Points Tracker

This tracks the requested cleanup work so the scope survives across implementation turns.

| Area            | Problem                                                                                                                      | Intended fix                                                                                                                                                                                                  | Status   |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Permissions     | CLI and daemon duplicate permission hashing, descriptor discovery, grant parsing, and SQLite schema/opening.                 | Added `@malleable/permission-core`; daemon owns permission writes through `PermissionBroker`, CLI permission commands call the daemon, and capsule state DB opening moved to `@malleable/capsule-state/node`. | Complete |
| Dependencies    | Lockfile contains duplicate generations of Vite, TypeScript, Rolldown, React plugin, and Lucide.                             | Normalized Rolldown peer/direct versions and ran clean `pnpm install`; lockfile now resolves one generation for the listed packages.                                                                          | Complete |
| Capsule schema  | Schema permits `solid` and `svelte`, but runtime refuses them.                                                               | Removed unsupported frameworks from the enum and deleted the now-dead daemon adapter throw.                                                                                                                   | Complete |
| Realms          | Shell renders only `realms[0]?.id` although APIs and CLI are multi-realm.                                                    | Added explicit selected realm state and realm selector; capsule data now loads per selected realm.                                                                                                            | Complete |
| Workbench       | Docs/log names mention workbench, but code has no concept for it.                                                            | Defined workbench in docs as the integration layer between external AI agents/tools and Malleable capsules.                                                                                                   | Complete |
| Shell structure | `apps/shell/src/main.tsx` mixes parsing, fetch, and UI, and redeclares schemas instead of using `@malleable/capsule-schema`. | Added `apps/shell/src/api.ts`; shell parsing composes exported capsule schemas and keeps request code out of the component.                                                                                   | Complete |
| Naming          | Permission/capability platform/broker and shell/web shell/console/system/platform naming drift.                              | Standardized docs/code around Shell, daemon, permission broker, capsule-system runtime, and workbench integration layer.                                                                                      | Complete |
| Templates       | Capsule create copies whole template directories with no skip policy.                                                        | Added `@malleable/template-core` with generated/dependency artifact skips and made CLI/daemon use it.                                                                                                         | Complete |
| Iframe sandbox  | Sandbox uses `allow-same-origin`, letting capsules call daemon APIs if auth cookies are later added.                         | Added a TODO at the sandbox site explaining the same-origin daemon boundary and isolated-origin direction.                                                                                                    | Complete |
| Logs            | `malleable capsule logs` shows permission events, not runtime logs.                                                          | Added daemon runtime log persistence/API and changed CLI logs to read runtime logs.                                                                                                                           | Complete |
